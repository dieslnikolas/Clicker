import { ScriptTypeToStringPipe } from './script-type-to-string.pipe';

describe('ScriptTypeToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ScriptTypeToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
